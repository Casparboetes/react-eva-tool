// // src/batches/BatchesContainer.test.js
// import React from 'react'
// import { shallow } from 'enzyme'
// import BatchesContainer from './BatchesContainer'
// import BatchItem from './BatchItem'
// import Title from '../components/Title'
// import { batches } from '../reducers/batches'
//
// describe('<BatchesContainer />', () => {
//   const shutup = true
//
//   it('shuts up', () => { expect(true).toEqual(shutup) })

  // const container = shallow(<BatchesContainer batches={batches} />)
  //
  // it('is wrapped in a div with class name "batches"', () => {
  //   expect(container).toHaveClassName('wrapper')
  //   expect(container).toHaveClassName('batches')
  // })
  //
  // it('sets the Title to "All Batches"', () => {
  //   expect(container).toContainReact(<Title content="Batches" />)
  // })
  //
  // it('renders all batches as a BatchItem', () => {
  //   batches.map((batch, index) => {
  //     return expect(container).toContainReact(<BatchItem key={index} { ...batch } onChange={() => {}} />)
  //   })
  // })
})
